# Copy this file to .env for local development and fill in your real key.
# Never commit the real .env file — .gitignore already excludes it.
# In production (Render), set this in Dashboard → your service → Environment instead.
WEB3FORMS_ACCESS_KEY=your-web3forms-access-key-here
